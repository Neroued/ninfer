# M2.8 Implementation Plan Index

> Status: planning index.
> Date: 2026-06-27.
> Scope: map the approved M2.8 pre-M3 standard into multiple bounded implementation plans.
> Authoritative standard: [`../m2.8-pre-m3-standard.md`](../m2.8-pre-m3-standard.md).
> Schema reference: [`../bench/e2e-report-schema.md`](../bench/e2e-report-schema.md).
> This file is **not** a Codex task list. It is the index that controls which detailed plan files should
> exist, what each plan owns, and how the plans depend on each other.

---

## 0. Current phase

The project is in **M2.8: pre-M3 benchmark / I/O / memory / observability gate**.

M2 correctness baseline is implemented and M2.5 hardening/documentation sync is mostly landed. M3 headline
per-kernel optimization must not begin until the M2.8 gate has produced auditable artifacts and
`docs/m3-readiness.md` exists.

M2.8 is not a performance-optimization phase. It is the phase that makes performance optimization safe,
measurable, reproducible, and auditable.

---

## 1. Global rules for every M2.8 plan

Every detailed plan under this index must follow these rules:

- Do **not** optimize kernels.
- Do **not** introduce kernel fusion.
- Do **not** introduce CUDA Graph capture/replay.
- Do **not** change sampler behavior beyond the existing greedy path.
- Do **not** add a C++ tokenizer.
- Do **not** make performance claims without a reproducible e2e report artifact.
- Keep real-weight e2e runs out of default CTest.
- Keep raw reports and profiler artifacts local under `profiles/` unless the standard explicitly says a
  committed summary is required.
- Separate correctness gates, hardening/observability gates, and performance evidence.
- When a plan changes q5090 tensor policy, converter behavior, runtime binding, and fixture behavior,
  those surfaces must be synchronized in the same plan or explicitly marked as remaining gaps.

---

## 2. Plan graph

```text
P1 q5090 canonical conv1d + hidden allocation closure
  ┐
P2 Arena / Engine memory observability
  ├── P6 First short observable baseline ── P7/P8 Workspace lifetime + M3 readiness package
P3 E2E benchmark binary + raw JSON report
  ┤
P4 Prompt fixtures + tokenizer/decode tooling
  ┤
P5 Report comparison + baseline summary tooling
  ┘
```

Parallelism policy:

- P2 is sequenced after P1 for the current implementation pass and should run directly in the current
  workspace once P1 is committed and the current workspace is clean; no separate worktree is required.
- P3 is sequenced after P2. Do not start P3 implementation until P2 is verified, the Engine memory stats
  API exists, and the workspace is clean.
- P4 is sequenced after P3 for the current implementation pass. Start P4 only after P3 is verified and
  the current workspace is clean.
- P5 is sequenced after P4 for the current implementation pass. Start P5 only after P4 is verified and
  the current workspace is clean.
- P6 is sequenced after P5 for the current implementation pass. Start P6 only after P5 is verified and
  the current workspace is clean. P6 is intentionally short-only because the current `step_reset` workspace
  policy cannot complete the current `long_2k` fixture within the device workspace budget.
- P7 and P8 are merged into one sequential implementation plan for the current pass. Start the merged
  P7/P8 plan only after P6 is verified and the current workspace is clean. The first `long_2k` observation
  happens after the P7 block-scoped workspace lifetime change.
- P8 steps inside the merged plan must wait until the P7 block-scoped workspace change, P7-vs-P6
  comparison, and P7 smoke summary are complete.

---

## 3. Plan status table

| Plan | File | Status | Blocks M3 gate? | Main output |
|---|---|---:|---:|---|
| P1 | `docs/plans/m2.8-p1-q5090-canonical-conv1d.md` | verified | yes | canonical conv1d q5090 path, no hidden official-path allocation |
| P2 | `docs/plans/m2.8-p2-engine-memory-observability.md` | verified | yes | arena high-water stats and Engine memory stats |
| P3 | `docs/plans/m2.8-p3-e2e-benchmark.md` | verified | yes | `qus_e2e_bench` and raw e2e JSON report generation |
| P4 | `docs/plans/m2.8-p4-fixtures-and-decode.md` | verified | yes | prompt fixture set, manifest, tokenizer/decode tooling |
| P5 | `docs/plans/m2.8-p5-report-compare-and-summary.md` | verified | yes | report validation/comparison and committed baseline summary tooling |
| P6 | `docs/plans/m2.8-p6-first-observable-baseline.md` | verified | yes | first short step_reset smoke baseline using current runtime policy |
| P7 | `docs/plans/m2.8-p7-p8-workspace-and-readiness.md` | planned | recommended gate blocker | block-scoped workspace lifetime and before/after comparison |
| P8 | `docs/plans/m2.8-p7-p8-workspace-and-readiness.md` | planned | yes | official M3 gate baseline and `docs/m3-readiness.md` |

`Status` values for this index should be one of:

```text
not written
planned
in progress
implemented
verified
superseded
```

Do not mark a plan `verified` from chat history alone. Verification must be based on repo files,
committed reports/summaries, or explicitly recorded commands/artifacts.

---

## 4. P1 — q5090 canonical conv1d + hidden allocation closure

Detailed plan file:

```text
docs/plans/m2.8-p1-q5090-canonical-conv1d.md
```

### Purpose

Make the canonical M2.8/M3 q5090 TEXT_CORE conv1d tensor policy real in converter, fixtures, runtime
binding, and tests.

Canonical target:

```text
linear_attn.conv1d.weight: contiguous BF16 logical shape [conv_dim, gdn_conv_k, 1] = [10240,4,1]
```

The official M3 baseline cannot use model-private raw `cudaMalloc` + D2D layout copy for transformed
conv1d storage.

### Owns

- `tools/q5090_convert/tensor_plan.py` conv1d logical-shape conversion.
- q5090 fixture generator conv1d shape.
- runtime `bind_conv1d_view` canonical path.
- tests that assert canonical conv1d shape and hidden-allocation policy.
- docs adjustments only if the implementation discovers the approved standard is not implementable.

### Does not own

- E2E benchmark binary.
- Arena high-water stats.
- Workspace lifetime policy.
- Causal conv1d kernel performance.
- General q5090 ABI redesign.

### Detailed implementation plan

The detailed implementation plan is now written at:

```text
docs/plans/m2.8-p1-q5090-canonical-conv1d.md
```

It covers converter tensor plan sync, fixture generator sync, runtime bind cleanup, hidden allocation
structure tests, documentation closeout, and regeneration of the real q5090 artifact from:

```text
/home/neroued/models/llm/qwen/Qwen3.6-27B/base-hf-bf16
```

### Completion signal

Canonical q5090 artifacts can be generated and consumed with `[10240,4,1]` conv1d tensors; the official
runtime path binds them as direct views; M3-gate reports can truthfully set:

```json
{
  "q5090_conv1d_layout": "runtime_native_conv_dim_by_kernel",
  "hidden_device_allocations": false
}
```

---

## 5. P2 — Arena / Engine memory observability

Detailed plan file:

```text
docs/plans/m2.8-p2-engine-memory-observability.md
```

### Purpose

Expose reliable memory accounting needed by e2e reports and future memory/lifetime hardening.

### Owns

- `DeviceArena::peak_used()`.
- `DeviceArena::reset_peak()`.
- High-water behavior across `alloc`, `rewind`, `reset`, failed allocation, and move operations.
- `WeightStore` stats needed by Engine reporting.
- `EngineMemoryStats` and `Engine::memory_stats()` / `Engine::reset_memory_peaks()`.
- Tests for all above behavior.

### Does not own

- Benchmark timing.
- JSON report writer.
- Loader redesign.
- Process RSS / CUDA internal memory accounting.
- Workspace lifetime changes.

### Detailed implementation plan

The detailed implementation plan is now written at:

```text
docs/plans/m2.8-p2-engine-memory-observability.md
```

It covers `DeviceArena` high-water marks, existing `WeightStore` q5090 counter coverage,
`EngineMemoryStats`, `Engine::memory_stats()`, `Engine::reset_memory_peaks()`, tests, and P2 status
closeout.

### Completion signal

A loaded or unloaded `Engine` can report dependency-free memory stats without synchronizing or initializing
CUDA; `qus_e2e_bench` can serialize weight/cache/workspace capacity, used, and peak bytes.

---

## 6. P3 — E2E benchmark binary + raw JSON report

Detailed plan file:

```text
docs/plans/m2.8-p3-e2e-benchmark.md
```

### Purpose

Implement the real-weight benchmark binary that drives the current Engine phase boundaries directly and
emits schema-versioned JSON reports.

### Owns

- `bench/e2e_bench.cpp` or equivalent.
- `qus_e2e_bench` CMake target.
- CLI defined by the standard.
- `.ids` parsing.
- `max_ctx` preflight validation.
- Host-visible `load_time_s`, `prefill_time_s`, `decode_time_s` timing.
- Warmup/measured repeat policy.
- Raw e2e JSON report writing.
- Error report writing when possible.

### Does not own

- Tokenizer or fixture generation.
- Decoded text sidecars.
- Report comparison.
- Kernel/device-only timing claims.
- CUDA Graph or no-host-feedback modes.

### Detailed implementation plan

The detailed implementation plan is now written at:

```text
docs/plans/m2.8-p3-e2e-benchmark.md
```

It covers support-layer parsing tests, `.ids` parsing, `max_ctx` preflight validation, schema-v1 raw JSON
writing, host-visible Engine phase timing, error-report smoke validation, and benchmark documentation.

### Completion signal

`qus_e2e_bench` can run one or more cases over a real q5090 file, report load/prefill/decode timing,
record generated token ids, record memory stats, and produce a schema-v1 JSON report under `profiles/e2e/`.

---

## 7. P4 — Prompt fixtures + tokenizer/decode tooling

Detailed plan file:

```text
docs/plans/m2.8-p4-fixtures-and-decode.md
```

### Purpose

Create stable, auditable real-text prompt inputs and a Python-only decode/readability path.

### Owns

- `bench/fixtures/prompts/*.txt`.
- `bench/fixtures/prompts/*.ids`.
- `bench/fixtures/prompts/m2.8-v1.manifest.json`.
- tokenizer provenance and tokenizer file hashes.
- prompt token counts and text/id hashes.
- `tools/bench/tokenize_prompts.py` or equivalent.
- `tools/bench/decode_e2e_report.py` or equivalent.
- decoded sidecar manifest generation.

### Does not own

- C++ tokenizer.
- E2E benchmark timing.
- Report comparison.
- Automated readability scoring.
- 8K/32K fixture expansion.

### Detailed implementation plan

The detailed implementation plan is now written at:

```text
docs/plans/m2.8-p4-fixtures-and-decode.md
```

It covers project-authored prompt text fixtures, deterministic long prompt generation, tokenizer helper
tooling, `.ids` regeneration/check, fixture manifest generation, decoded sidecar tooling, docs, and
verification.

### Completion signal

The `m2.8-v1` fixture set is committed and auditable, `.ids` files are canonical C++ inputs, and decoded
text artifacts can be generated from an e2e report for human smoke inspection.

---

## 8. P5 — Report comparison + baseline summary tooling

Detailed plan file:

```text
docs/plans/m2.8-p5-report-compare-and-summary.md
```

### Purpose

Make e2e reports comparable and allow raw local reports to produce committed audit summaries.

### Owns

- Report schema validation.
- `tools/bench/compare_e2e_reports.py`.
- Default hard failure and warning policy.
- Optional flags to promote perf/memory warnings to failures.
- `tools/bench/make_baseline_summary.py` or equivalent.
- Committed `docs/bench/baselines/*.json` summary format.

### Does not own

- Running benchmarks.
- Producing prompt fixtures.
- Decoding generated text.
- Claiming performance improvements.

### Detailed implementation plan

The detailed implementation plan is now written at:

```text
docs/plans/m2.8-p5-report-compare-and-summary.md
```

It covers schema validation, comparison hard failures and warnings, warning promotion flags, token-id
comparison policy, committed baseline summary generation, docs, and synthetic-report verification.

### Completion signal

Two schema-v1 reports can be compared; token/schema/identity errors are hard failures; perf/memory
regressions are warnings by default; official raw reports can generate committed audit summaries.

---

## 9. P6 — First short observable baseline

Detailed plan file:

```text
docs/plans/m2.8-p6-first-observable-baseline.md
```

### Purpose

Exercise the M2.8 toolchain and capture the first short observable baseline before workspace lifetime
changes.

### Owns

- Smoke e2e run over `cn_short`.
- Raw report paths under `profiles/e2e/`.
- Decoded text sidecars for at least the short case.
- Committed smoke/observation summary under `docs/bench/baselines/` if useful.
- Recording `workspace_lifetime_policy = "step_reset"`.

### Does not own

- M3 gate readiness.
- Workspace lifetime changes.
- Long-prefill observation over `long_2k`; the current `step_reset` runtime cannot complete the current
  long fixture within the device workspace budget, so P7 owns the first post-rewind `long_2k` observation.
- Kernel optimization.
- Retuning benchmark schema.

### Detailed implementation plan

The detailed implementation plan is now written at:

```text
docs/plans/m2.8-p6-first-observable-baseline.md
```

It covers P5-gated preflight, the real-q5090 short smoke run, raw local report handling, decoded sidecar
generation, redacted decoded manifest handling, committed smoke summary generation, and P6 verification
closeout.

### Completion signal

The benchmark/decode/summary chain works end to end. The result is allowed to be a smoke or observation
baseline; it does not by itself satisfy the M3 gate.

---

## 10. P7 — Workspace lifetime hardening

Detailed plan file:

```text
docs/plans/m2.8-p7-p8-workspace-and-readiness.md
```

### Purpose

After a before-baseline exists, move workspace temporary lifetime from coarse step-level reset toward the
approved block-scoped mixer/MLP rewind policy.

### Owns

- L2 temporary tensor liveness audit.
- `attn_mix` / `gdn_mix` / `mlp_tail` lifetime map.
- block-scoped `mark()` / `rewind()` implementation.
- tap ordering invariants.
- same-stream reuse invariants.
- before/after e2e memory/timing comparison.

### Does not own

- Kernel optimization.
- Fusion.
- CUDA Graph capture.
- Changing tap semantics.
- Changing numerical behavior.

### Detailed implementation plan

P7 is now merged with P8 in one sequential detailed plan:

```text
docs/plans/m2.8-p7-p8-workspace-and-readiness.md
```

The P7 portion covers preflight against the P6 short `step_reset` before-baseline, block-scoped mark/rewind
implementation, structural tests for tap/rewind ordering, P7 smoke run over the full fixture set including
the first valid `long_2k` observation, P7-vs-P6 comparison over the shared short case, and a committed P7
smoke summary.

### Completion signal

Reports can use `workspace_lifetime_policy = "block_scoped_mixer_mlp_rewind"`; correctness/parity remains
stable; workspace peak changes are measured and recorded.

---

## 11. P8 — M3 readiness package

Detailed plan file:

```text
docs/plans/m2.8-p7-p8-workspace-and-readiness.md
```

### Purpose

Produce the official M3-entry evidence package and record the first-wave M3 target ordering without
starting optimization.

### Owns

- Official M3-gate e2e baseline run.
- Required `cn_short` and `long_2k` cases.
- Raw local report under `profiles/e2e/`.
- Committed baseline summary under `docs/bench/baselines/`.
- `docs/m3-readiness.md`.
- First-wave M3 target ordering by expected global impact.

### Does not own

- M3 kernel implementation tasks.
- Performance claims beyond recorded baseline facts.
- Any waiver not reflected in the M2.8 standard.

### Detailed implementation plan

P8 is now merged with P7 in one sequential detailed plan:

```text
docs/plans/m2.8-p7-p8-workspace-and-readiness.md
```

The P8 portion starts only after the P7 block-scoped workspace change and smoke summary are complete. It
covers the official M3-gate e2e baseline, decoded sidecars, committed M3 gate summary, `docs/m3-readiness.md`,
and final index closeout.

### Completion signal

`docs/m3-readiness.md` exists and records report path/SHA, committed summary path, git commit, q5090
identity, prompt cases, memory accounting scope, hidden allocation status, workspace policy, and first M3
target ordering. Only then may M3 headline per-kernel optimization planning begin.

---

## 12. Open discussion points before writing detailed plans

These are policy questions to settle before or while writing the detailed per-plan files:

1. **Should P1 always be first?**
   Recommended: yes. Otherwise early baselines are legacy smoke only and cannot satisfy the M3 gate.

2. **Is P7 a hard M3 blocker or a recommended hardening gate?**
   Resolved for the current implementation pass by merging P7 and P8. P8 cannot start inside the merged plan
   until P7 has produced the block-scoped runtime policy, comparison, and committed smoke summary.

3. **Should P6 produce a committed summary or only local reports?**
   Recommended: produce at least one committed smoke summary once the toolchain works, because it tests the
   summary flow before the official M3 gate baseline.

4. **Should P5 be implemented before P6?**
   Required for the current implementation pass. P6 uses P5 validation and summary tooling, so start P6
   only after P5 is verified.

---

## 13. Next document to write

The next detailed plan is already written:

```text
docs/plans/m2.8-p7-p8-workspace-and-readiness.md
```

The merged P7/P8 plan is next because the short `step_reset` before-baseline now exists and the first
`long_2k` observation is intentionally deferred until after P7 block-scoped workspace rewind.

No separate P7 or P8 document should be written. After P7/P8 verification, M3 optimization planning may
begin from `docs/m3-readiness.md`.

#pragma once
// qus::core — DType enum and element sizes (BF16, FP32, I32, U8=packed-4bit storage).
// L0 infrastructure. See docs/l0-infrastructure-design.md §4.
// NOTE: structure stub — no implementation yet.

namespace qus {

// TODO(impl): enum class DType : uint8_t { BF16, FP32, I32, U8 };
// TODO(impl): size_t dtype_size(DType);

}  // namespace qus

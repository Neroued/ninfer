// qus::kernels — full GQA attention (stub). See include/qus/kernels/attention.h.
#include "qus/kernels/attention.h"

// TODO(impl): prefill (causal flash-style) + decode (single-query vs KV cache), scale 1/sqrt(256).

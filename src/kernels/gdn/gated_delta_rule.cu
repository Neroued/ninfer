// qus::kernels — Gated-DeltaNet core (stub). See include/qus/kernels/gdn.h.
#include "qus/kernels/gdn.h"

// TODO(impl): recurrent (decode) state update + chunked WY/UT (prefill); gdn_gating; l2norm.
